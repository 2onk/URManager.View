<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="URManager.View.Controls.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:URManager.View.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{StaticResource HeaderBackgroundBrush}">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
            <Image Source="/Images/logo.png" 
                   Width="120" 
                   Margin="5"/>
            <TextBlock Text="UR Manager" 
                       FontSize="30" 
                       Foreground="{StaticResource HeaderForegroundBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Version 1.0" 
                       FontSize="14"
                       Foreground="#333333" VerticalAlignment="Bottom" Margin="10"/>
        </StackPanel>
    </Grid>
</Page>
